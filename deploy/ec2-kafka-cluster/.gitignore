*.js
*.d.ts
node_modules

# CDK asset staging directory
.cdk.staging
cdk.out

# Generated certs/keystores (never commit)
certs/

# Cluster config (contains IPs, generated by deploy.sh)
.cluster-env

# Benchmark results (local collection)
results/

# CDK context cache (VPC/AZ lookups — changes every deploy)
cdk.context.json
