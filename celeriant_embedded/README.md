# celeriant_embedded

Reserved for future use.

This crate will provide an embedded mode for Celeriant - running the database in-process without a TCP server. Applications will be able to link directly against the engine and issue queries via a local API, bypassing all network overhead.

Currently a placeholder stub with no real functionality.
